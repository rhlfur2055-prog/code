# 🚀 MSA 프로젝트 구축 - 빠른 명령어 치트시트

## ⚡ 한 줄 명령 프롬프트

### 1. VSCode 설정 확인
```
"VSCode 설정이 제대로 적용됐는지 확인해줘"
```

### 2. 프로젝트 구조 생성
```
"MSA 프로젝트 폴더 구조를 만들어줘. frontend, spring-api, python-ai, mysql, scripts 폴더 포함해서"
```

### 3. Docker Compose 개발 환경
```
"docker-compose.dev.yml 만들어줘. Next.js, Spring Boot, FastAPI, MySQL, phpMyAdmin 서비스 포함"
```

### 4. Dockerfile 생성 (모든 서비스)
```
"Next.js, Spring Boot, FastAPI 각각의 Dockerfile을 만들어줘. 멀티스테이지 빌드 사용"
```

### 5. 배포 스크립트
```
"setup.sh, healthcheck.sh, backup.sh 스크립트를 만들어줘. Windows용 ps1도 같이"
```

### 6. 환경 변수 설정
```
".env.example 파일을 만들어줘. MySQL, Spring, FastAPI, Next.js 설정 모두 포함"
```

### 7. 헬스 체크 엔드포인트
```
"각 서비스에 헬스 체크 엔드포인트를 만들어줘. Spring은 /actuator/health, FastAPI는 /health"
```

### 8. Git 설정
```
".gitignore, .gitattributes, README.md 만들어줘"
```

### 9. 서비스 실행
```
"Docker 이미지 빌드하고 서비스 실행해줘. 헬스 체크까지"
```

### 10. 프로덕션 준비
```
"docker-compose.prod.yml 만들고 보안 설정, 리소스 제한 추가해줘"
```

---

## 🎯 시나리오별 프롬프트

### 처음 시작하는 경우
```
"MSA 프로젝트를 처음부터 끝까지 만들어줘.
VSCode 설정부터 Docker 구성, 스크립트, 환경 변수까지 전부.
각 단계마다 설명도 해줘"
```

### 빠르게 진행하고 싶은 경우
```
"README-DEPLOY에 있는 모든 파일을 한 번에 만들어줘.
docker-compose, Dockerfile, 스크립트, 환경 설정 전부"
```

### 특정 부분만 수정하고 싶은 경우
```
"docker-compose.dev.yml만 다시 만들어줘.
MySQL 설정을 PostgreSQL로 변경하고 Redis 추가해서"
```

---

## 📋 체크리스트 프롬프트

```
"지금까지 만든 파일들을 체크리스트로 정리해줘.
README-DEPLOY와 비교해서 빠진 게 있는지 확인하고
완성도 퍼센트도 계산해줘"
```

---

## 🐛 에러 해결

```
"이런 에러가 났어: [에러 메시지]
원인 분석하고 해결 방법 알려줘"
```

---

## 🔄 수정 프롬프트

```
"[파일명]을 열어서 [수정 내용]으로 바꿔줘"

예:
"docker-compose.dev.yml을 열어서 MySQL 포트를 3307로 바꿔줘"
```

---

## 💡 다음에 물어볼 것들

1. **개발 환경 실행**
   ```
   "개발 환경을 실행하는 방법을 단계별로 알려줘"
   ```

2. **로그 확인**
   ```
   "각 서비스의 로그를 확인하는 방법 알려줘"
   ```

3. **디버깅**
   ```
   "VSCode에서 Docker 컨테이너를 디버깅하는 설정 만들어줘"
   ```

4. **데이터베이스 백업**
   ```
   "MySQL 데이터를 백업하는 방법 알려줘"
   ```

5. **서비스 재시작**
   ```
   "특정 서비스만 재시작하는 명령어 알려줘"
   ```

---

## 🎨 커스터마이징

```
"내 프로젝트 구성은:
- Frontend: [기술]
- Backend: [기술]
- Database: [기술]

이걸로 docker-compose 만들어줘"
```

---

## 📱 음성 명령 (그대로 읽기)

✅ "브이에스코드 설정 확인해줘"
✅ "엠에스에이 프로젝트 구조 만들어줘"
✅ "도커 컴포즈 개발 환경 만들어줘"
✅ "모든 서비스 도커파일 생성해줘"
✅ "배포 스크립트 만들어줘"
✅ "점 이엔브이 예제 파일 만들어줘"
✅ "헬스 체크 엔드포인트 구현해줘"
✅ "깃 설정 파일 만들어줘"
✅ "서비스 실행해줘"
✅ "지금까지 만든 거 체크리스트로 보여줘"
