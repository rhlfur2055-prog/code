# -*- coding: utf-8 -*-
"""
97개 알고리즘 콘텐츠 생성기
각 주제별 고유한 콘텐츠를 생성합니다.
"""

import json
import sys

sys.stdout.reconfigure(encoding='utf-8')

# 각 알고리즘별 콘텐츠 정의
ALGORITHM_CONTENTS = {
    # ===== 01_기초 =====
    "01_기초/algorithm-intro": {
        "title": "알고리즘 입문",
        "description": "알고리즘의 정의와 중요성, 좋은 알고리즘의 조건을 학습합니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🔥 알고리즘이란?",
                "content": """## 🔥 한 줄 요약
> **문제를 해결하는 단계별 절차** - 요리 레시피처럼 입력을 출력으로 바꾸는 명확한 규칙

---

## 💡 왜 배워야 하나?

### 실무에서:
- **성능 최적화**: 같은 기능도 알고리즘에 따라 1초 vs 1시간 차이
- **비용 절감**: AWS 서버 비용이 알고리즘 효율에 직결
- **확장성**: 사용자 100명 → 100만명 될 때 버티는 코드

### 코딩테스트에서:
- 출제 빈도: ⭐⭐⭐⭐⭐ (모든 문제의 기본)
- 대표 기업: 삼성, 카카오, 네이버, 라인 **전부**

### 이걸 모르면:
- ❌ "왜 내 코드는 시간초과지?" 평생 의문
- ❌ 면접에서 "시간복잡도가 뭐예요?" 대답 못함
- ❌ 대용량 데이터 처리 불가능

---

## 🎯 핵심 개념 (5분 컷)

### 📚 요리 레시피로 이해하기

**라면 끓이기 알고리즘:**
```
입력: 라면, 물 500ml, 스프
출력: 맛있는 라면

1. 냄비에 물 500ml를 넣는다
2. 물이 끓으면 면과 스프를 넣는다
3. 4분 30초 기다린다
4. 불을 끄고 그릇에 담는다
```

**핵심 포인트 3가지:**
1. **입력(Input)**: 문제에서 주어지는 데이터
2. **출력(Output)**: 우리가 구해야 하는 답
3. **과정(Process)**: 입력을 출력으로 바꾸는 단계

### 좋은 알고리즘의 조건
| 조건 | 설명 | 예시 |
|-----|------|-----|
| **정확성** | 올바른 결과 도출 | 1+1=2 (O), 1+1=3 (X) |
| **효율성** | 빠르고 메모리 적게 | O(n) vs O(n²) |
| **명확성** | 누구나 이해 가능 | 주석 잘 달린 코드 |"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 알고리즘의 기본 구조 예시
# 문제: 1부터 N까지의 합 구하기

def sum_basic(n):
    \"\"\"방법 1: 반복문 - O(n)\"\"\"
    total = 0
    for i in range(1, n + 1):
        total += i
    return total

def sum_formula(n):
    \"\"\"방법 2: 수학 공식 - O(1)\"\"\"
    return n * (n + 1) // 2

# 테스트
print(sum_basic(100))   # 5050
print(sum_formula(100)) # 5050

# 같은 결과, 다른 효율!
# n = 1억일 때:
# - sum_basic: 수 초 소요
# - sum_formula: 즉시 완료"""
            },
            {
                "type": "best-practice",
                "title": "✅ Good vs ❌ Bad",
                "content": """**1️⃣ 문제 이해**

❌ **Bad:** 바로 코딩 시작
```python
# 문제 안 읽고 코딩 시작...
def solve():
    pass  # 뭘 해야 하지?
```

✅ **Good:** 입력/출력 먼저 파악
```python
# 입력: 정수 N (1 ≤ N ≤ 100)
# 출력: 1부터 N까지의 합
def solve(n):
    return n * (n + 1) // 2
```

---

**2️⃣ 알고리즘 설계**

❌ **Bad:** 무작정 코딩
```python
# 일단 해보자...
for i in range(n):
    for j in range(n):
        for k in range(n):
            # O(n³) 지옥
```

✅ **Good:** 시간복잡도 먼저 계산
```python
# N = 10만이면 O(n²)은 100억 연산 = 시간초과
# O(n log n) 이하로 설계해야 함
```"""
            },
            {
                "type": "practice",
                "title": "🎯 연습 문제",
                "content": """### Level 1: 기초
| 문제 | 플랫폼 | 포인트 |
|-----|-------|-------|
| A+B (1000) | 백준 | 입출력 기초 |
| Hello World (2557) | 백준 | 첫 제출 연습 |

### Level 2: 응용
| 문제 | 플랫폼 | 포인트 |
|-----|-------|-------|
| 합 구하기 (11720) | 백준 | 문자열 → 숫자 |
| 최댓값 (2562) | 백준 | 배열 순회 |

### ✅ 체크리스트
- [ ] 알고리즘 = 문제 해결 절차임을 안다
- [ ] 입력/출력을 먼저 파악한다
- [ ] 시간복잡도 개념을 안다"""
            }
        ]
    },

    "01_기초/time-complexity": {
        "title": "시간 복잡도",
        "description": "Big-O 표기법과 시간 복잡도 분석 방법을 학습합니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🔥 시간 복잡도란?",
                "content": """## 🔥 한 줄 요약
> **입력 크기에 따른 연산 횟수의 증가율** - "N이 커지면 얼마나 느려지나?"

---

## 💡 왜 배워야 하나?

### 실무에서:
- **서버 비용**: O(n²) 코드 하나가 월 서버비 10배 증가시킴
- **사용자 경험**: 3초 이상 걸리면 사용자 이탈
- **코드 리뷰**: "이거 시간복잡도 얼마예요?" 필수 질문

### 코딩테스트에서:
- 출제 빈도: ⭐⭐⭐⭐⭐ (매 문제 필수 고려)
- 시간 제한 1초 = 약 1억 연산
- N ≤ 10만 → O(n log n) 이하 필요

---

## 🎯 핵심 개념 (5분 컷)

### 📚 택배 배송으로 이해하기

**O(1) - 상수 시간**: 드론 배송
> "어디든 1시간" - 거리 무관

**O(log n) - 로그 시간**: 이진 탐색 배송
> "절반씩 좁혀서 찾기" - 1024개 중 10번만에 찾음

**O(n) - 선형 시간**: 일반 택배
> "집집마다 방문" - 100집이면 100번

**O(n²) - 제곱 시간**: 모든 집 서로 비교
> "100집 × 100집 = 10,000번"

### Big-O 비교표
| 표기법 | N=10 | N=100 | N=1000 | 특징 |
|-------|------|-------|--------|-----|
| O(1) | 1 | 1 | 1 | 해시 조회 |
| O(log n) | 3 | 7 | 10 | 이진 탐색 |
| O(n) | 10 | 100 | 1000 | 단순 반복 |
| O(n log n) | 30 | 700 | 10000 | 정렬 |
| O(n²) | 100 | 10000 | 1000000 | 이중 반복 |
| O(2ⁿ) | 1024 | 💀 | 💀 | 부분집합 |"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 시간 복잡도별 코드 예시

# O(1) - 상수 시간
def get_first(arr):
    return arr[0]  # 항상 1번 연산

# O(log n) - 로그 시간
def binary_search(arr, target):
    left, right = 0, len(arr) - 1
    while left <= right:  # 매번 절반으로 줄어듦
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1

# O(n) - 선형 시간
def find_max(arr):
    max_val = arr[0]
    for num in arr:  # N번 반복
        if num > max_val:
            max_val = num
    return max_val

# O(n²) - 제곱 시간
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):        # N번
        for j in range(n-1):  # × N번 = N²
            if arr[j] > arr[j+1]:
                arr[j], arr[j+1] = arr[j+1], arr[j]
    return arr

# O(2ⁿ) - 지수 시간 (피하자!)
def fibonacci_recursive(n):
    if n <= 1:
        return n
    return fibonacci_recursive(n-1) + fibonacci_recursive(n-2)"""
            },
            {
                "type": "tip",
                "title": "💡 시간복잡도 빠르게 계산하는 팁",
                "content": """### 1초 = 1억 연산 법칙

| N의 범위 | 허용 시간복잡도 | 대표 알고리즘 |
|---------|--------------|-------------|
| N ≤ 10 | O(N!) | 순열 완전탐색 |
| N ≤ 20 | O(2ᴺ) | 비트마스킹 |
| N ≤ 500 | O(N³) | 플로이드 워셜 |
| N ≤ 5,000 | O(N²) | 버블/선택 정렬 |
| N ≤ 100,000 | O(N log N) | 퀵/병합 정렬 |
| N ≤ 10,000,000 | O(N) | 투 포인터, 해시 |

### 꿀팁
```python
# 시간복잡도 빠르게 파악하기
for i in range(n):           # O(n)
    for j in range(n):       # O(n²)
        for k in range(n):   # O(n³)

for i in range(n):
    for j in range(i):       # O(n²/2) = O(n²)

while n > 0:
    n //= 2                  # O(log n)
```"""
            },
            {
                "type": "practice",
                "title": "🎯 연습 문제",
                "content": """### 시간복잡도 맞추기 퀴즈

**Q1.** 다음 코드의 시간복잡도는?
```python
for i in range(n):
    for j in range(n, 0, -1):
        print(i, j)
```
<details><summary>정답</summary>O(n²) - i가 n번, j도 n번</details>

**Q2.** N=100,000일 때 1초 안에 통과 가능한 것은?
- A) O(n²)
- B) O(n log n)
- C) O(n³)

<details><summary>정답</summary>B) O(n log n) = 약 170만 연산</details>

### 백준 추천 문제
| 난이도 | 문제 | 포인트 |
|-------|-----|-------|
| ⭐ | 알고리즘 수업 - 점근적 표기 1 (24313) | Big-O 개념 |
| ⭐⭐ | 수 정렬하기 (2750) | O(n²) 정렬 |
| ⭐⭐⭐ | 수 정렬하기 2 (2751) | O(n log n) 필요 |"""
            }
        ]
    },

    "01_기초/space-complexity": {
        "title": "공간 복잡도",
        "description": "메모리 사용량 분석과 공간 복잡도 최적화 방법을 학습합니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🔥 공간 복잡도란?",
                "content": """## 🔥 한 줄 요약
> **알고리즘이 사용하는 메모리의 양** - "얼마나 많은 변수/배열을 만드나?"

---

## 💡 왜 배워야 하나?

### 실무에서:
- **서버 메모리 제한**: EC2 인스턴스 메모리 초과 시 OOM 에러
- **모바일 앱**: 메모리 부족하면 앱 강제 종료
- **임베디드**: IoT 기기는 메모리 KB 단위

### 코딩테스트에서:
- 메모리 제한: 보통 256MB ~ 512MB
- int 배열 1억 개 = 약 400MB
- 재귀 깊이 제한 = 스택 메모리

---

## 🎯 핵심 개념 (5분 컷)

### 📚 사무실 책상으로 이해하기

**O(1) - 상수 공간**: 책상 위 메모지 1장
> "변수 몇 개만 사용" - 입력 크기 무관

**O(n) - 선형 공간**: 파일 캐비닛
> "입력만큼 저장" - N개 데이터 → N칸 필요

**O(n²) - 제곱 공간**: 창고
> "2차원 배열" - N×N 표 저장

### 공간 복잡도 계산
```
메모리 사용량 = 고정 공간 + 가변 공간

고정 공간: 코드, 상수, 고정 변수
가변 공간: 입력에 따라 달라지는 공간 (이게 중요!)
```

### 자료형별 메모리
| 자료형 | Python | Java | C++ |
|-------|--------|------|-----|
| int | 28 bytes | 4 bytes | 4 bytes |
| float | 24 bytes | 8 bytes | 8 bytes |
| 배열[N] | N × 28 | N × 4 | N × 4 |"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 공간 복잡도별 코드 예시

# O(1) - 상수 공간
def sum_formula(n):
    # 변수 1개만 사용
    return n * (n + 1) // 2

# O(n) - 선형 공간
def create_list(n):
    # N개 요소를 가진 리스트 생성
    return [i for i in range(n)]

# O(n) - 재귀도 공간 사용!
def factorial_recursive(n):
    # 재귀 깊이 N = 스택 프레임 N개
    if n <= 1:
        return 1
    return n * factorial_recursive(n - 1)

# O(1) - 반복문으로 공간 절약
def factorial_iterative(n):
    result = 1
    for i in range(2, n + 1):
        result *= i
    return result

# O(n²) - 2차원 배열
def create_matrix(n):
    # N × N 크기 행렬
    return [[0] * n for _ in range(n)]

# 공간 복잡도 개선 예시
# ❌ O(n) 공간
def fibonacci_memo(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    memo[n] = fibonacci_memo(n-1) + fibonacci_memo(n-2)
    return memo[n]

# ✅ O(1) 공간
def fibonacci_optimized(n):
    if n <= 1:
        return n
    prev, curr = 0, 1
    for _ in range(2, n + 1):
        prev, curr = curr, prev + curr
    return curr"""
            },
            {
                "type": "best-practice",
                "title": "✅ 공간 최적화 기법",
                "content": """**1️⃣ 불필요한 복사 피하기**

❌ **Bad:**
```python
def process(arr):
    new_arr = arr.copy()  # O(n) 추가 공간
    # 처리...
    return new_arr
```

✅ **Good:**
```python
def process(arr):
    # 원본 수정이 가능하면 in-place로
    for i in range(len(arr)):
        arr[i] *= 2
    return arr
```

---

**2️⃣ 제너레이터 활용**

❌ **Bad:**
```python
def get_squares(n):
    return [i**2 for i in range(n)]  # O(n) 공간
```

✅ **Good:**
```python
def get_squares(n):
    for i in range(n):
        yield i**2  # O(1) 공간
```

---

**3️⃣ 슬라이딩 윈도우**

❌ **Bad:**
```python
# 모든 부분합 저장
prefix = [0] * (n + 1)
for i in range(n):
    prefix[i+1] = prefix[i] + arr[i]
```

✅ **Good:**
```python
# 현재 윈도우만 유지
window_sum = sum(arr[:k])
for i in range(k, n):
    window_sum += arr[i] - arr[i-k]
```"""
            },
            {
                "type": "practice",
                "title": "🎯 연습 문제",
                "content": """### 공간 복잡도 분석 퀴즈

**Q1.** 다음 코드의 공간복잡도는?
```python
def mystery(n):
    arr = [0] * n
    for i in range(n):
        arr[i] = i * 2
    return arr
```
<details><summary>정답</summary>O(n) - 크기 n인 배열 생성</details>

**Q2.** 재귀 호출 깊이가 N인 함수의 공간복잡도는?
<details><summary>정답</summary>O(n) - 스택 프레임 N개 사용</details>

### 최적화 연습
| 문제 | 힌트 |
|-----|-----|
| 피보나치를 O(1) 공간으로 | 변수 2개만 사용 |
| 행렬 회전을 in-place로 | swap 활용 |
| 연속 부분합 O(1) 공간 | 슬라이딩 윈도우 |"""
            }
        ]
    },

    "01_기초/recursion-basic": {
        "title": "재귀 기초",
        "description": "재귀 함수의 개념과 기본 패턴을 학습합니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🔥 재귀란?",
                "content": """## 🔥 한 줄 요약
> **함수가 자기 자신을 호출하는 것** - 큰 문제를 같은 형태의 작은 문제로 쪼개기

---

## 💡 왜 배워야 하나?

### 실무에서:
- **트리/그래프 탐색**: 폴더 구조, DOM 탐색, 조직도
- **분할 정복**: 병합 정렬, 퀵 정렬
- **JSON 파싱**: 중첩된 객체 처리

### 코딩테스트에서:
- 출제 빈도: ⭐⭐⭐⭐⭐ (DFS, 백트래킹 필수)
- 대표 유형: 순열/조합, 트리 순회, 그래프 탐색

---

## 🎯 핵심 개념 (5분 컷)

### 📚 러시아 인형(마트료시카)로 이해하기

```
큰 인형을 열면 → 작은 인형이 나오고
작은 인형을 열면 → 더 작은 인형이 나오고
...
가장 작은 인형 → 더 이상 열 수 없음 (기저 조건!)
```

### 재귀의 3요소
1. **기저 조건(Base Case)**: 재귀를 멈추는 조건 ⚠️ 필수!
2. **재귀 호출(Recursive Call)**: 자기 자신 호출
3. **문제 축소**: 매 호출마다 문제가 작아져야 함

### 팩토리얼로 이해하기
```
5! = 5 × 4!
   = 5 × 4 × 3!
   = 5 × 4 × 3 × 2!
   = 5 × 4 × 3 × 2 × 1!
   = 5 × 4 × 3 × 2 × 1  ← 기저 조건: 1! = 1
   = 120
```"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 재귀 기본 패턴

# 1. 팩토리얼
def factorial(n):
    # 기저 조건: n이 1 이하면 1 반환
    if n <= 1:
        return 1
    # 재귀 호출: n × (n-1)!
    return n * factorial(n - 1)

print(factorial(5))  # 120

# 2. 피보나치 (비효율적 버전 - 이해용)
def fibonacci(n):
    # 기저 조건
    if n <= 1:
        return n
    # 재귀 호출
    return fibonacci(n - 1) + fibonacci(n - 2)

print(fibonacci(10))  # 55

# 3. 배열의 합
def array_sum(arr):
    # 기저 조건: 빈 배열
    if not arr:
        return 0
    # 첫 요소 + 나머지 배열의 합
    return arr[0] + array_sum(arr[1:])

print(array_sum([1, 2, 3, 4, 5]))  # 15

# 4. 문자열 뒤집기
def reverse_string(s):
    # 기저 조건
    if len(s) <= 1:
        return s
    # 마지막 문자 + 나머지 뒤집기
    return s[-1] + reverse_string(s[:-1])

print(reverse_string("hello"))  # "olleh"

# 5. 거듭제곱 (분할 정복)
def power(base, exp):
    if exp == 0:
        return 1
    if exp % 2 == 0:
        half = power(base, exp // 2)
        return half * half
    else:
        return base * power(base, exp - 1)

print(power(2, 10))  # 1024"""
            },
            {
                "type": "common-mistake",
                "title": "⚠️ 재귀 초보자 실수 TOP 3",
                "content": """**1️⃣ 기저 조건 누락 → 무한 재귀**

❌ **Bad:**
```python
def countdown(n):
    print(n)
    countdown(n - 1)  # 언제 멈춰?! → RecursionError
```

✅ **Good:**
```python
def countdown(n):
    if n <= 0:  # 기저 조건!
        return
    print(n)
    countdown(n - 1)
```

---

**2️⃣ 문제가 축소되지 않음**

❌ **Bad:**
```python
def wrong_factorial(n):
    if n <= 1:
        return 1
    return n * wrong_factorial(n)  # n이 그대로!
```

✅ **Good:**
```python
def factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)  # n-1로 축소
```

---

**3️⃣ 재귀 깊이 초과**

```python
import sys
sys.setrecursionlimit(10000)  # Python 기본: 1000

# 깊은 재귀가 필요하면 반복문으로 변환 고려
def factorial_iterative(n):
    result = 1
    for i in range(2, n + 1):
        result *= i
    return result
```"""
            },
            {
                "type": "practice",
                "title": "🎯 연습 문제",
                "content": """### Level 1: 기초
| 문제 | 플랫폼 | 포인트 |
|-----|-------|-------|
| 팩토리얼 (10872) | 백준 | 기본 재귀 |
| 피보나치 수 5 (10870) | 백준 | 두 갈래 재귀 |

### Level 2: 응용
| 문제 | 플랫폼 | 포인트 |
|-----|-------|-------|
| 하노이 탑 이동 순서 (11729) | 백준 | 재귀 사고력 |
| 별 찍기 - 10 (2447) | 백준 | 분할 정복 |

### 직접 구현해보기
1. 1부터 N까지 합을 재귀로 구현
2. 배열에서 최댓값을 재귀로 찾기
3. 이진수 변환을 재귀로 구현"""
            }
        ]
    },

    "01_기초/recursion-advanced": {
        "title": "재귀 심화",
        "description": "꼬리 재귀, 메모이제이션 등 재귀 최적화 기법을 학습합니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🔥 재귀 최적화",
                "content": """## 🔥 한 줄 요약
> **재귀의 단점을 극복하는 기법들** - 메모이제이션, 꼬리 재귀, 반복문 변환

---

## 💡 왜 배워야 하나?

### 재귀의 문제점:
1. **스택 오버플로우**: 깊은 재귀 시 에러
2. **중복 계산**: 같은 값 반복 계산
3. **느린 속도**: 함수 호출 오버헤드

### 해결책:
- **메모이제이션**: 계산 결과 저장 → O(2ⁿ) → O(n)
- **꼬리 재귀**: 스택 사용 최소화
- **반복문 변환**: 재귀를 루프로

---

## 🎯 핵심 개념

### 1. 메모이제이션 (Memoization)
```
fib(5)를 구하면:
- 일반 재귀: fib(3) 2번, fib(2) 3번 중복 계산
- 메모이제이션: 한 번 계산한 건 저장해서 재사용
```

### 2. 꼬리 재귀 (Tail Recursion)
```
일반 재귀: return n * factorial(n-1)
          → 돌아와서 곱셈 필요 (스택 유지)

꼬리 재귀: return factorial(n-1, acc * n)
          → 바로 반환 가능 (스택 최적화 가능)
```"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 재귀 최적화 기법

# 1. 메모이제이션 - 데코레이터 사용
from functools import lru_cache

@lru_cache(maxsize=None)
def fib_memo(n):
    if n <= 1:
        return n
    return fib_memo(n - 1) + fib_memo(n - 2)

print(fib_memo(100))  # 즉시 계산!

# 2. 메모이제이션 - 딕셔너리 사용
def fib_dict(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    memo[n] = fib_dict(n - 1, memo) + fib_dict(n - 2, memo)
    return memo[n]

# 3. 꼬리 재귀 (Python은 최적화 안 하지만 개념 이해용)
def factorial_tail(n, accumulator=1):
    if n <= 1:
        return accumulator
    return factorial_tail(n - 1, accumulator * n)

# 4. 반복문으로 변환 (가장 효율적)
def fib_iterative(n):
    if n <= 1:
        return n
    prev, curr = 0, 1
    for _ in range(2, n + 1):
        prev, curr = curr, prev + curr
    return curr

# 5. 타뷸레이션 (Bottom-Up DP)
def fib_tabulation(n):
    if n <= 1:
        return n
    dp = [0] * (n + 1)
    dp[1] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    return dp[n]

# 성능 비교
import time

n = 35
# 일반 재귀: 수 초 소요
# 메모이제이션: 즉시
# 반복문: 즉시"""
            },
            {
                "type": "tip",
                "title": "💡 재귀 vs 반복문 선택 가이드",
                "content": """### 언제 재귀를 쓸까?

✅ **재귀가 좋은 경우:**
- 트리/그래프 탐색 (DFS)
- 분할 정복 (퀵정렬, 병합정렬)
- 백트래킹 (순열, 조합)
- 코드 가독성이 중요할 때

❌ **반복문이 좋은 경우:**
- 단순 반복 작업
- 깊이가 매우 깊을 때 (N > 1000)
- 성능이 중요할 때
- 메모리가 제한적일 때

### 변환 패턴
```python
# 재귀 → 반복문 변환 공식
def recursive(n):
    if base_condition:
        return base_value
    return combine(recursive(n-1))

# ↓ 변환

def iterative(n):
    result = base_value
    for i in range(appropriate_range):
        result = combine(result)
    return result
```"""
            },
            {
                "type": "practice",
                "title": "🎯 연습 문제",
                "content": """### 최적화 연습

**Q1.** 메모이제이션으로 개선하기
```python
# 이 코드를 최적화하세요
def count_ways(n):
    if n <= 1:
        return 1
    return count_ways(n-1) + count_ways(n-2) + count_ways(n-3)
```

**Q2.** 반복문으로 변환하기
```python
# 이 재귀를 반복문으로 바꾸세요
def sum_recursive(n):
    if n <= 0:
        return 0
    return n + sum_recursive(n - 1)
```

### 백준 추천 문제
| 난이도 | 문제 | 포인트 |
|-------|-----|-------|
| ⭐⭐ | 피보나치 함수 (1003) | 재귀 호출 횟수 |
| ⭐⭐⭐ | 타일 채우기 (2133) | DP로 변환 |
| ⭐⭐⭐ | 파도반 수열 (9461) | 메모이제이션 |"""
            }
        ]
    },

    "01_기초/quiz-complexity": {
        "title": "복잡도 퀴즈",
        "description": "시간/공간 복잡도 분석 실전 연습 문제입니다.",
        "language": "Python",
        "sections": [
            {
                "type": "concept",
                "title": "🎯 복잡도 퀴즈",
                "content": """## 🔥 복잡도 마스터 퀴즈

이 퀴즈를 통해 시간/공간 복잡도 분석 능력을 테스트하세요!

---

### 퀴즈 1: 기본 반복문

```python
def mystery1(n):
    count = 0
    for i in range(n):
        for j in range(n):
            count += 1
    return count
```
**시간복잡도?** <details><summary>정답</summary>O(n²)</details>

---

### 퀴즈 2: 중첩 조건

```python
def mystery2(n):
    count = 0
    for i in range(n):
        for j in range(i):
            count += 1
    return count
```
**시간복잡도?** <details><summary>정답</summary>O(n²) - n*(n-1)/2 ≈ n²/2</details>

---

### 퀴즈 3: 로그 시간

```python
def mystery3(n):
    count = 0
    while n > 0:
        count += 1
        n //= 2
    return count
```
**시간복잡도?** <details><summary>정답</summary>O(log n)</details>"""
            },
            {
                "type": "code",
                "language": "Python",
                "code": """# 🔰 복잡도 분석 연습 코드

# 퀴즈 4: 이 함수의 시간복잡도는?
def find_pairs(arr):
    n = len(arr)
    pairs = []
    for i in range(n):
        for j in range(i + 1, n):
            pairs.append((arr[i], arr[j]))
    return pairs
# 정답: O(n²)

# 퀴즈 5: 이 함수의 시간복잡도는?
def binary_search_count(arr, target):
    left, right = 0, len(arr) - 1
    count = 0
    while left <= right:
        mid = (left + right) // 2
        count += 1
        if arr[mid] == target:
            return count
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return count
# 정답: O(log n)

# 퀴즈 6: 이 함수의 공간복잡도는?
def create_spiral(n):
    matrix = [[0] * n for _ in range(n)]
    # 나선형으로 채우는 로직...
    return matrix
# 정답: O(n²)

# 퀴즈 7: 재귀의 시간복잡도는?
def fib(n):
    if n <= 1:
        return n
    return fib(n-1) + fib(n-2)
# 정답: O(2ⁿ) - 지수 시간!

# 퀴즈 8: 이건?
def fib_memo(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    memo[n] = fib_memo(n-1, memo) + fib_memo(n-2, memo)
    return memo[n]
# 정답: O(n) - 메모이제이션으로 개선"""
            },
            {
                "type": "practice",
                "title": "🎯 실전 문제",
                "content": """### 난이도별 복잡도 분석

**Level 1: 빈칸 채우기**

| 코드 패턴 | 시간복잡도 |
|----------|----------|
| `for i in range(n): O(1)` | O(___) |
| `for i in range(n): for j in range(n):` | O(___) |
| `while n > 0: n //= 2` | O(___) |
| `arr.sort()` | O(___) |
| `set.add(x)` | O(___) |

<details><summary>정답</summary>
O(n), O(n²), O(log n), O(n log n), O(1)
</details>

---

**Level 2: 코드 분석**

```python
def process(arr):
    arr.sort()  # ?
    for x in arr:  # ?
        if x in arr:  # ?
            print(x)
```
**총 시간복잡도?**
<details><summary>정답</summary>
O(n log n) + O(n) × O(n) = O(n²)
(in 연산이 리스트에서 O(n))
</details>

---

**Level 3: 최적화 문제**

"N개 숫자 중 중복을 제거하라"
- 방법 1: 이중 루프 비교 → O(?)
- 방법 2: 정렬 후 인접 비교 → O(?)
- 방법 3: Set 사용 → O(?)

<details><summary>정답</summary>
O(n²), O(n log n), O(n)
</details>"""
            }
        ]
    },
}

# 나머지 알고리즘 콘텐츠 계속...
# (파일이 너무 길어져서 2부로 나눕니다)

def generate_content():
    """algorithm.json 파일 업데이트"""
    filepath = r"c:\tools\codemaster-next-main\codemaster-next-main\codemaster-next-main\src\data\contents\algorithm.json"

    with open(filepath, 'r', encoding='utf-8') as f:
        data = json.load(f)

    updated_count = 0

    for key, content in ALGORITHM_CONTENTS.items():
        if key in data:
            data[key]["description"] = content["description"]
            data[key]["language"] = content["language"]
            data[key]["isPlaceholder"] = False
            data[key]["sections"] = content["sections"]
            updated_count += 1
            print(f"✓ Updated: {key}")

    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

    print(f"\n총 {updated_count}개 항목 업데이트 완료!")
    return updated_count

if __name__ == "__main__":
    generate_content()
